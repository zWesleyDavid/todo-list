version: "3.1"

services:
  db:
    image: postgres:latest
    environment:
      POSTGRES_DB: taskdb
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5400:5432"
